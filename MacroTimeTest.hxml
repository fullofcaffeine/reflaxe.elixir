# Macro-Time Compiler Unit Tests
# These tests run AT COMPILE TIME and test the real compiler

-cp src
-cp test
-lib reflaxe
-D reflaxe_runtime

# This will run the tests during compilation!
--macro test.MacroTimeCompilerTest.main()

# No output needed - tests run at compile time
--no-output