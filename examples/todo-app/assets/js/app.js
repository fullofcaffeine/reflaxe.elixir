// Haxe-generated client bundle (hooks, etc).
// LiveSocket bootstrap happens in `assets/js/phoenix_app.js` by default.
// Optional: compiling the Haxe client with `-D todoapp_hx_live_socket_bootstrap` can connect from Haxe;
// `phoenix_app.js` has a guard to prevent double connections.
import "./hx_app.js";
