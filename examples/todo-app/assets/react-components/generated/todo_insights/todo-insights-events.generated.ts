/* Generated by PhoenixHx LiveReactEventProtocol. Do not edit. */
export const LIVE_EVENT_PROTOCOL_HASH = "5c83cd675a3cb5900b517b6dffb17945402c8f91" as const
export const LIVE_EVENT_PROTOCOL_MANIFEST = "protocol shared.liveview.TodoInsightsEvent\ncompanion TodoInsightsEvents\nevent hook SetFilter todo_insights_filter (payload:shared.liveview.TodoInsightsFilterInput{filter:String->filter:string})\n" as const

/** Structural subset accepted by stock LiveReact's broader pushEvent function. */
export type LiveReactPushEvent = (
  event: string,
  payload: Record<string, unknown>,
) => unknown

const hasOwn = (value: Record<string, unknown>, key: string): boolean =>
  Object.prototype.hasOwnProperty.call(value, key)

function expectRecord(value: unknown, label: string): Record<string, unknown> {
  if (value === null || typeof value !== "object" || Array.isArray(value)) {
    throw new Error(label + " must be an object")
  }
  return value as Record<string, unknown>
}

function expectExactKeys(
  value: Record<string, unknown>,
  required: readonly string[],
  optional: readonly string[],
  label: string,
): void {
  const allowed = new Set([...required, ...optional])
  for (const key of Object.keys(value)) {
    if (!allowed.has(key)) {
      throw new Error(label + " has unexpected field " + key)
    }
  }
  for (const key of required) {
    if (!hasOwn(value, key)) {
      throw new Error(label + " is missing field " + key)
    }
  }
}

function expectString(value: unknown, label: string): string {
  if (typeof value !== "string") {
    throw new Error(label + " must be a string")
  }
  return value
}

export const SET_FILTER_EVENT = "todo_insights_filter" as const
export interface SetFilterInput {
  readonly filter: string
}

export function decodeSetFilterInput(value: unknown): SetFilterInput {
  const input = expectRecord(value, "shared.liveview.TodoInsightsEvent.SetFilter")
  expectExactKeys(
    input,
    ["filter"],
    [],
    "shared.liveview.TodoInsightsEvent.SetFilter",
  )
  return {
    filter: expectString(input["filter"], "shared.liveview.TodoInsightsEvent.SetFilter.filter"),
  }
}

export function encodeSetFilterPayload(input: SetFilterInput): Record<string, unknown> {
  return {
    filter: input.filter,
  }
}

export function pushSetFilter(pushEvent: LiveReactPushEvent, candidate: SetFilterInput): void {
  const input = decodeSetFilterInput(candidate)
  pushEvent(SET_FILTER_EVENT, encodeSetFilterPayload(input))
}
