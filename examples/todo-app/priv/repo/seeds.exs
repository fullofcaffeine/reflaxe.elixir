# Optional database seeds for the example app.
#
# This file intentionally contains no seeding logic. The `mix setup` and
# `mix dev` aliases run `ecto.setup`, which includes `run priv/repo/seeds.exs`.
# Keeping this file present avoids alias failures while remaining a no‑op.

:ok

