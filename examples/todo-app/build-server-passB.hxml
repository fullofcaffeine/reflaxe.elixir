# Pass B — domain (schemas/migrations/presence)
-lib reflaxe.elixir
-cp src_haxe
-cp src_haxe/server
-cp src_haxe/shared
-D elixir_output=lib
-D reflaxe_runtime
-D no-utf16
-D app_name=TodoApp
-dce full
--macro exclude('client')
--macro exclude('test')
-D hxx_string_to_sigil
-D no-traces
-D no_traces
server.schemas.Todo
server.migrations.CreateTodos
server.presence.TodoPresence
