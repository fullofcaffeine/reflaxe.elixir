defimpl Drawable, for: String do
  def raw(value) do
    value.draw()
  end

  def rea(value) do
    value.area()
  end

end


defimpl Drawable, for: String do
  def raw(value) do
    value.draw()
  end

  def rea(value) do
    value.area()
  end

end
