# Haxe→JavaScript compilation for Phoenix LiveView client boot (Genes)

-cp src_haxe/client
-cp src_haxe

-lib genes-ts
-lib phoenix_js

# Use temp output + promotion to avoid watch races while esbuild watches imports.
-js assets/js/_hx_app_tmp.js
-D js-unflatten
--dce=full

-D real-position
-D js-source-map

--macro exclude('server')

-main client.Boot
