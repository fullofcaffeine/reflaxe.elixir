--- a/tests/unit/src/unitstd/List.unit.hx
+++ b/test/upstream_unitstd/upstream/List.unit.hx
@@ -46,6 +46,16 @@
 l4.add(3);
 l4.add(5);
 l4.add(8);
-[for (k=>v in l4) k] == [0,1,2,3,4];
-[for (k=>v in l4) v] == [1,2,3,5,8];
-[for (k=>v in l4) k*v] == [0,2,6,15,32];
\ No newline at end of file
+var keys = [];
+var values = [];
+var products = [];
+var keyValueIterator = l4.keyValueIterator();
+while (keyValueIterator.hasNext()) {
+	var entry = keyValueIterator.next();
+	keys.push(entry.key);
+	values.push(entry.value);
+	products.push(entry.key * entry.value);
+}
+keys == [0,1,2,3,4];
+values == [1,2,3,5,8];
+products == [0,2,6,15,32];
