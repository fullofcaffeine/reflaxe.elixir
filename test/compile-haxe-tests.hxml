# Proper test configuration for compiling test Haxe files to Elixir
# This tests the compiler by actually using it to generate Elixir code
# NOT by trying to instantiate it at runtime

-cp test/fixtures
-cp src

# Include reflaxe library
-lib reflaxe

# Use the ElixirCompiler as a macro (correct approach)
--macro reflaxe.elixir.CompilerInit.Start()

# Output directory for generated Elixir files
-D elixir_output=test-output/compiler-tests

# Compile multiple test classes
SimpleTestClass
TestLiveView
TestGenServer