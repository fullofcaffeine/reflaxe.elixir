defmodule EReg do
  def new(r, opt) do
    %{}
  end
  def match(struct, s) do
    false
  end
  def matched(struct, n) do
    nil
  end
  def matched_right(struct) do
    nil
  end
  def replace(struct, s, by) do
    nil
  end
end