defmodule ChainedAccess do
  defp test() do
    root = Node.new()
    left = Node.new()
    right = Node.new()
    if (_this = root.left
if (_this == nil), do: 0, else: _this.value >= _this = root.right
if (_this == nil), do: 0, else: _this.value) do
      Log.trace("left >= right", %{:fileName => "ChainedAccess.hx", :lineNumber => 25, :className => "ChainedAccess", :methodName => "test"})
    end
  end
  defp main() do
    test()
  end
end