# Test configuration for RouterBuildMacro with INVALID action reference
# Should produce specific warning about missing action method

# Include Reflaxe.Elixir source
-cp src
-cp ../../../../std  
-lib reflaxe

# Source files to compile
-cp test/snapshot/phoenix/RouterBuildMacro_InvalidAction

# Output directory
-D elixir_output=test/snapshot/phoenix/RouterBuildMacro_InvalidAction/out

# Required for Reflaxe targets
-D reflaxe_runtime

# Elixir platform configuration
-D no-utf16

# Define library version and initialize compiler
-D reflaxe.elixir=0.1.0
--macro reflaxe.elixir.CompilerInit.Start()

# Main class
Main