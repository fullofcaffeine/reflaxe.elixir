# Test configuration for RouterBuildMacro with VALID controller/action references
# Should produce NO warnings during compilation

# Include Reflaxe.Elixir source
-cp src
-cp ../../../../std  
-lib reflaxe

# Source files to compile
-cp test/snapshot/phoenix/RouterBuildMacro_ValidController

# Output directory
-D elixir_output=test/snapshot/phoenix/RouterBuildMacro_ValidController/out

# Required for Reflaxe targets
-D reflaxe_runtime

# Elixir platform configuration
-D no-utf16

# Define library version and initialize compiler
-D reflaxe.elixir=0.1.0
--macro reflaxe.elixir.CompilerInit.Start()

# Main class
Main