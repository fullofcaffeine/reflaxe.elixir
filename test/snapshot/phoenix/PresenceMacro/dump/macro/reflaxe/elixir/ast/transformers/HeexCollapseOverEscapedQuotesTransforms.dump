class reflaxe.elixir.ast.transformers.HeexCollapseOverEscapedQuotesTransforms {

	public static function transformPass(ast:reflaxe.elixir.ast.ElixirAST) {
		return reflaxe.elixir.ast.ElixirASTTransformer.transformNode(ast, function(n:reflaxe.elixir.ast.ElixirAST) {
			return @:ast(switch (n.def) {
	case ESigil(type, content, modifiers) if (type == "H"):
		var updated = collapse(content);
		if (updated != content) makeASTWithMeta(ESigil(type, updated, modifiers), n.metadata, n.pos) else n;	
	default:
		n;	
}) {
				var ` = n.def;
				if (enumIndex ` == 61) {
					var ` = `[0];
					var ` = `[1];
					var ` = `[2];
					{
						var type = `;
						var content = `;
						var modifiers = `;
						if (type == "H") {
							var updated = reflaxe.elixir.ast.transformers.HeexCollapseOverEscapedQuotesTransforms.collapse(content);
							if (updated != content) {
								{def : reflaxe.elixir.ast.ElixirASTDef.ESigil(type, updated, modifiers), metadata : n.metadata, pos : n.pos};
							} else {
								n;
							};
						} else {
							n;
						};
					};
				} else {
					n;
				};
			};
		});
	}

	static function collapse(s:String) {
		var buf = new StringBuf();
		var i = 0;
		while (i < s.length) {
			if (i + 2 < s.length && s.charAt(i) == "\\" && s.charAt(i + 1) == "\\" && s.charAt(i + 2) == "\"") {
				buf.add("\\\"");
				i += 3;
				continue;
			};
			if (i + 1 < s.length && s.charAt(i) == "\\" && s.charAt(i + 1) == "\"") {
				buf.add("\"");
				i += 2;
				continue;
			};
			buf.add(s.charAt(i));
			i ++;
		};
		return buf.toString();
	}
}