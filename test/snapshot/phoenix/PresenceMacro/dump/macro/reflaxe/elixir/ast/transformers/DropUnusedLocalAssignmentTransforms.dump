class reflaxe.elixir.ast.transformers.DropUnusedLocalAssignmentTransforms {

	public static function pass(ast:reflaxe.elixir.ast.ElixirAST) {
		return reflaxe.elixir.ast.ElixirASTTransformer.transformNode(ast, function(n:reflaxe.elixir.ast.ElixirAST) {
			return @:ast(switch (n.def) {
	case EBlock(stmts):
		makeASTWithMeta(EBlock(rewrite(stmts)), n.metadata, n.pos);	
	case EDo(stmts2):
		makeASTWithMeta(EDo(rewrite(stmts2)), n.metadata, n.pos);	
	default:
		n;	
}) {
				var ` = n.def;
				switch (enumIndex `) {
					case 53: {
						var ` = `[0];
						{
							var stmts = `;
							{
								{
									var def = reflaxe.elixir.ast.ElixirASTDef.EBlock(reflaxe.elixir.ast.transformers.DropUnusedLocalAssignmentTransforms.rewrite(stmts));
									var meta = n.metadata;
									var pos = n.pos;
									{def : def, metadata : meta, pos : pos};
								};
							};
						};
					};
					case 55: {
						var ` = `[0];
						{
							var stmts2 = `;
							{
								{
									var def = reflaxe.elixir.ast.ElixirASTDef.EDo(reflaxe.elixir.ast.transformers.DropUnusedLocalAssignmentTransforms.rewrite(stmts2));
									var meta = n.metadata;
									var pos = n.pos;
									{def : def, metadata : meta, pos : pos};
								};
							};
						};
					};
					default: {
						n;
					}
				};
			};
		});
	}

	static function rewrite(stmts:Array<reflaxe.elixir.ast.ElixirAST>) {
		if (stmts == null) {
			return stmts;
		};
		var out = [];
		{
			var ` = 0;
			var ` = stmts.length;
			while (` < `) {
				var i = ` ++;
				var s = stmts[i];
				var replaced = null;
				@:ast(switch (s.def) {
	case EBinary(Match, left, rhs):
		switch (left.def) {
			case EVar(name):
				if (!usedLater(stmts, i + 1, name)) {
					if (name.length > 1 && name.charAt(0) == "_") {
						var base = name.substr(1);
						if (usedLater(stmts, i + 1, base)) {
							replaced = makeASTWithMeta(EBinary(Match, makeASTWithMeta(EVar(base), left.metadata, left.pos), rhs), s.metadata, s.pos);
						} else {
							replaced = rhs;
						};
					} else {
						replaced = rhs;
					};
				};			
			default:
		};	
	case EMatch(PVar(name2), rhs2):
		if (!usedLater(stmts, i + 1, name2)) {
			if (name2.length > 1 && name2.charAt(0) == "_") {
				var base2 = name2.substr(1);
				if (usedLater(stmts, i + 1, base2)) {
					replaced = makeASTWithMeta(EMatch(PVar(base2), rhs2), s.metadata, s.pos);
				} else {
					replaced = rhs2;
				};
			} else {
				replaced = rhs2;
			};
		};	
	default:
}) {
					var ` = s.def;
					switch (enumIndex `) {
						case 8: {
							var ` = `[0];
							var ` = `[1];
							if (enumIndex ` == 0) {
								var ` = `[0];
								{
									var name2 = `;
									var rhs2 = `;
									{
										if (! reflaxe.elixir.ast.transformers.DropUnusedLocalAssignmentTransforms.usedLater(stmts, i + 1, name2)) {
											if (name2.length > 1 && name2.charAt(0) == "_") {
												var base2 = name2.substr(1, null);
												if (reflaxe.elixir.ast.transformers.DropUnusedLocalAssignmentTransforms.usedLater(stmts, i + 1, base2)) {
													replaced = {def : reflaxe.elixir.ast.ElixirASTDef.EMatch(reflaxe.elixir.ast.EPattern.PVar(base2), rhs2), metadata : s.metadata, pos : s.pos};
												} else {
													replaced = rhs2;
												};
											} else {
												replaced = rhs2;
											};
										};
									};
								};
							} else {};
						};
						case 26: {
							var ` = `[0];
							var ` = `[1];
							var ` = `[2];
							if (enumIndex ` == 27) {
								{
									var left = `;
									var rhs = `;
									{
										@:ast(switch (left.def) {
	case EVar(name):
		if (!usedLater(stmts, i + 1, name)) {
			if (name.length > 1 && name.charAt(0) == "_") {
				var base = name.substr(1);
				if (usedLater(stmts, i + 1, base)) {
					replaced = makeASTWithMeta(EBinary(Match, makeASTWithMeta(EVar(base), left.metadata, left.pos), rhs), s.metadata, s.pos);
				} else {
					replaced = rhs;
				};
			} else {
				replaced = rhs;
			};
		};	
	default:
}) {
											var ` = left.def;
											if (enumIndex ` == 38) {
												var ` = `[0];
												{
													var name = `;
													{
														if (! reflaxe.elixir.ast.transformers.DropUnusedLocalAssignmentTransforms.usedLater(stmts, i + 1, name)) {
															if (name.length > 1 && name.charAt(0) == "_") {
																var base = name.substr(1, null);
																if (reflaxe.elixir.ast.transformers.DropUnusedLocalAssignmentTransforms.usedLater(stmts, i + 1, base)) {
																	replaced = {def : reflaxe.elixir.ast.ElixirASTDef.EBinary(reflaxe.elixir.ast.EBinaryOp.Match, {def : reflaxe.elixir.ast.ElixirASTDef.EVar(base), metadata : left.metadata, pos : left.pos}, rhs), metadata : s.metadata, pos : s.pos};
																} else {
																	replaced = rhs;
																};
															} else {
																replaced = rhs;
															};
														};
													};
												};
											} else {};
										};
									};
								};
							} else {};
						};
						default: {}
					};
				};
				out.push(if (replaced != null) {
					replaced;
				} else {
					s;
				});
			};
		};
		return out;
	}

	static function usedLater(stmts:Array<reflaxe.elixir.ast.ElixirAST>, start:Int, name:String) {
		var found = [false];
		{
			var ` = start;
			var ` = stmts.length;
			while (` < `) {
				var j = ` ++;
				if (! found[0]) {
					reflaxe.elixir.ast.ASTUtils.walk(stmts[j], function(x:reflaxe.elixir.ast.ElixirAST) {
						@:ast(switch (x.def) {
	case EVar(v) if (v == name):
		found = true;	
	default:
}) {
							var ` = x.def;
							if (enumIndex ` == 38) {
								var ` = `[0];
								{
									var v = `;
									if (v == name) {
										found[0] = true;
									} else {};
								};
							} else {};
						};
					});
				};
			};
		};
		return found[0];
	}
}