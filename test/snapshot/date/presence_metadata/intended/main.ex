defmodule Main do
  def main() do
    meta = %{:online_at => this1 = DateTime.utc_now()
this1.to_unix("millisecond"), :user_name => "alice", :avatar => nil}
    Log.trace(Std.string(meta), nil)
  end
end