Main.hx:66: characters 3-17 : Reflaxe.Elixir cannot preserve this shared Array mutation. The current lowering would update only `values`, so `alias.length` would read the old Array. Avoid the shared alias. For example, compute a new Array with `concat(...)` and use that binding. This diagnostic covers one narrow pattern. Other shared aliases remain unsupported.
