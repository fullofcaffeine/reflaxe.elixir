/**
 * List (Elixir target)
 *
 * Haxe std defines `List<T>` as an alias of `haxe.ds.List<T>`.
 * We mirror that here so the module exists under the Elixir-target std overrides.
 */
typedef List<T> = haxe.ds.List<T>;

