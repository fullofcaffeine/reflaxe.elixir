# Compatibility alias for projects that still use `-lib genes`.
#
# This file intentionally contains no compiler classpath. Both the legacy name
# and new `-lib genes-ts` name resolve the same immutable genes-ts revision.
-lib genes-ts
-D genes=1.36.7
