# Comprehensive Reflaxe.Elixir Test Suite
# Architecture: npm/lix manages Haxe compiler deps, tests the compiler itself
# Separate: mix manages Elixir runtime deps, tests generated code

-cp src
-cp test
-lib reflaxe
-lib tink_unittest 
-lib tink_testrunner
-D reflaxe_runtime

# Run comprehensive test runner that includes:
# - Core extern definition tests
# - Modern tink_unittest tests with performance validation
# - Feature-based test organization
--main test.ComprehensiveTestRunner
--interp